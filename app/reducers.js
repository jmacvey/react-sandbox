import { combineReducers } from 'redux';

const app = (state = {}, action) => {
    switch (action.type) {
        default:
            return state;//
    }
}

export const Reducers = combineReducers({
    app
});